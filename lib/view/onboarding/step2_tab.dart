import 'package:flutter/material.dart';

class Step2Tab extends StatelessWidget {
  const Step2Tab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [Image.asset('assets/images/onboarding/step2.png')],
      ),
    );
  }
}
